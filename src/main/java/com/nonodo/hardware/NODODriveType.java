package com.nonodo.hardware;

/**
 * Which drivetrain to use. Pick one in Java the same way Blocks picks
 * {@code initializeMecanumDrive} vs {@code initializeTankDrive}.
 */
public enum NODODriveType {
    MECANUM,
    TANK
}
